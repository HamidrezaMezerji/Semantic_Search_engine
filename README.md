# Semantic_Search_engine
an end-to-end closed semantic retrieval that load text files, chunks them, generates embeddings for them, indexes them in a vector space and retrieves Documents based on Semantic Similarity rather than exact words matching.
